<!-- ANSWER KEY — grader-only. Never feed this file to the agent under test;
     the seed PRD is seed-1-lowfi.md.

     Kept in plain text on purpose. The bot cannot fetch docs/evals/ through
     github_read or source_read (src/integrations/repo-read-guard.ts), and the
     guard holds at every ref — including the historical ones an encrypted tip
     left reachable. A rubric a reviewer cannot read is a rubric nobody
     checks, which is how a case asserted the wrong thing for eight days. -->

PLANTED GAPS (answer key — the missing-context gate must fire on at least one):
1. No unit for "quantity" — weight, count, or bunches? Three crops in one plot
   answer differently, and a log that mixes them cannot be summed.
2. Nothing says what a member sees in a week with no entries yet, which is the
   state a brand-new garden is in for its whole first week.
3. "A coordinator can open an entry" is the only mention of a role. Whether a
   member may edit or delete an entry they filed — their own or anyone's — is
   unstated, and so is who becomes a coordinator.

NOT gaps (a spec may settle these from the PRD as written):
- The four recorded fields; browsing scoped to the current week; the existence
  of an entry-detail view.
