---
summary: Criteria, tests, fixtures, results — one folder, one loop
---

# evals/ — the quality loop (L4)

Criteria, tests, fixtures, results — one folder, one loop.

| Dir | Holds | Rule |
|---|---|---|
| `rubrics/` | what "good" means, one rubric per artifact type (PRD, prototype, share-out, lesson, skill, agent-spec, bot-answer D1–D9) | YAML criteria + prose rationale; applied by `reviewers/rubric-applier` |
| `scenarios/` | `<skill>.md` — ≥3 scenarios (query + expected behavior) per skill | **written BEFORE the skill body** (evals-first); baseline without the skill, then write the minimal skill that passes |
| `fixtures/` | frozen golden inputs + answer keys per skill (see fixtures/README.md) | benchmarks run against these — a moving fixture measures nothing |
| `runs/` | `*.jsonl` — one line per scored run, plus `apply-log.jsonl` (one row per executed Flow-5 verdict: target · verdict link · timestamp) | interim store until the Notion Eval Runs DB exists; written at flow exits |

Populated 2026-07-07: 7 rubrics (six skills + bot-answer) rebuilt from the Notion 📊 Evals doc's metric targets, and 7 scenario sets (six skills, evals-first, + the bot regression prompts R1–R12, migrated from the former `agents/uno-bot/REGRESSION.md`, since removed). NOTE: `fixtures/uno-bot-cases.json` — what the `uno-bot — evals` Action actually runs — carries **29 cases**, all executed by `agents/uno-bot/scripts/run-evals.mjs`: R1–R12 plus R20 (13 R-cases), B1 (a tooling question — "where do we use Zoom?" — reaching the touchpoint registry through `search_blueprint` with `include: ["touchpoints"]`, added 2026-09-05 with #414; the runner's `expectToolCalled` matches a list arg by membership since then), P1–P6 (prototype-method regression guards; P2/P5 rotate the provenance coverage added 2026-08-31; P6, added 2026-09-04 with #419, proves a pasted Figma URL reaches `prototype_scaffold` from the tool descriptions alone, and since 2026-09-05 with #418 also that the turn reads `uno-prototype/method` through `read_reference` first — the last method to leave the prompt) and S1–S3, T1–T2 (tier and thinking level, 2026-09-04), M1 (a maintain turn reaching its disclosed method through `read_reference`, 2026-09-05 with #423), D1 (a publish turn reaching `uno-publish/method` the same way, 2026-09-05 with #424), V1 (a review turn reaching `uno-review/method`, added 2026-09-05 with #426; `uno-research/method` and `uno-synthesize/method` have no case of their own yet), and C1 (clearing, 2026-09-05 with #426: a two-turn thread whose turn 1 reads `uno-maintain/method`, asserting that the history the runner sends to turn 2 carries the reference RECEIPT on the turn that read it and stays under 8000 chars — the method alone is ~10k, so the text cannot have travelled; the route reports the receipt from the same `withTurnScope` production persists it from, and `expectHistory` is the fixture key). Its LLM-judge answers live only in the encrypted sibling fixture and are loaded with an Actions secret, so the bot under test cannot retrieve its own grading instructions through `github_read`. This line read "16 total" until 2026-08-27; R20 and S1–S3 were added without it, which is how a count in prose drifts from a count in a file (#249). bot-answer dimensions D1/D4 were never recorded anywhere recoverable; their definitions are reconstructed as inferred-and-canonical in the rubric (2026-07-08).
