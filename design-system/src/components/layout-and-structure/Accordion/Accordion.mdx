import { DocsTabs, DocsTab } from '@/storybook-docs/docs-tabs.jsx';
import { Meta, Title, Description, Canvas } from '@storybook/addon-docs/blocks';

import * as AccordionStories from './Accordion.stories.jsx';
import { DocsCanvasShell, DocsInteractivePlayground, ResourcesBlock, DsCanvasQuiet } from '@/storybook-docs/ds-docs-layout.jsx';

export const figmaMeta = {
  "fileKey": "zAecJNRdvJzAUOcjV32tRX",
  "sets": [
    {
      "id": "accordion",
      "name": "accordion",
      "componentSetNodeId": "13667:6004",
      "url": "https://www.figma.com/design/zAecJNRdvJzAUOcjV32tRX/Design-System---BS4-Foundation--Component-LIbrary-?node-id=13667-6004",
      "source": "node-chart",
      "status": "verified"
    }
  ]
};

<Meta of={AccordionStories} />

<Title />
Accordion component for organizing content into collapsible sections. Built on React Bootstrap with custom styling per the design system.

<DocsTabs of={AccordionStories}>

<DocsTab tab="examples">

<ResourcesBlock 
    figmaLink="https://www.figma.com/design/zAecJNRdvJzAUOcjV32tRX/Design-System---BS4-Foundation--Component-LIbrary-?node-id=13265-5813"
    githubLink="https://github.com/BilLogic/plus-vibe-coding-starting-kit/tree/main/design-system/src/components/layout-and-structure/Accordion"
    sets={figmaMeta.sets}
    figmaFileKey={figmaMeta.fileKey}
/>

<div className="sb-ds-component-docs sb-ds-component-docs--page not-prose">

<div className="sb-ds-doc-section">

## Overview

<DocsCanvasShell attachSourceBelow description={<>Composable children instead of the `items` array.</>}>
    <Canvas of={AccordionStories.Overview} story={{ inline: true }} layout="padded" sourceState="hidden" />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Content

<DocsCanvasShell description={<>Children-based items pattern alongside the <code>items</code> array API.</>}>
    <DsCanvasQuiet of={AccordionStories.Content} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Appearance

<DocsCanvasShell description={<>Default bordered accordion and flush (borderless) treatment.</>}>
    <DsCanvasQuiet of={AccordionStories.Styles} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Behavior

<DocsCanvasShell description={<>Disclosure <strong>model</strong>: multiple panels open at once (`alwaysOpen`) and an initial state where every panel starts collapsed (no default active key).</>}>
    <DsCanvasQuiet of={AccordionStories.Behavior} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## States

<DocsCanvasShell description={<>Per-header affordances on the trigger (here: <code>disabled</code>). Expanded vs collapsed is <strong>disclosure state</strong> (which keys are open), separate from disabled, hover, focus, or pressed styling on the header.</>}>
    <DsCanvasQuiet of={AccordionStories.InteractionStates} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Interactive playground

<DocsInteractivePlayground
    of={AccordionStories.Interactive}
    description={
        <>
            Use the table below to change props. The preview is rendered{' '}
            <strong className="text-foreground">inline</strong> while you scroll the docs page.
        </>
    }
/>

</div>

</div>

</DocsTab>

<DocsTab tab="usage">

<div className="sb-ds-component-docs sb-ds-component-docs--page not-prose">

<div className="sb-ds-doc-section">

## When to use

Use `Accordion` when a page carries several long, independent sections and only
one of them matters at a time — a settings page, a FAQ, the optional detail
under a form. Each item is collapsed until asked for, so the page keeps its
shape and the headers stay a readable index of what is there.

It is uncontrolled by default: give it `defaultActiveKey` and it tracks the open
item itself. Pass `activeKey` and `onSelect` together to control it. `alwaysOpen`
lets more than one item stay open at once.

</div>

<div className="sb-ds-doc-section">

## Usage notes

### There are two ways to author items, and `children` wins.

```jsx
// Incorrect — the items array is dropped without warning. The component
// returns children whenever children exist, and never looks at items.
<Accordion items={sections}>
    <Accordion.Item eventKey="0" header="One">…</Accordion.Item>
</Accordion>

// Correct — pick one. The array for uniform, data-driven sections:
<Accordion items={sections} />

// or children when a body needs its own markup:
<Accordion>
    <Accordion.Item eventKey="0" header="One">…</Accordion.Item>
</Accordion>
```

### `alwaysOpen` needs an array key, even for one item.

```jsx
// Incorrect — the open item snaps shut on the first click elsewhere. With
// alwaysOpen set and a non-array key, opening any item replaces the whole
// selection with just that item.
<Accordion alwaysOpen defaultActiveKey="0" items={sections} />

// Correct.
<Accordion alwaysOpen defaultActiveKey={['0']} items={sections} />
```

### `flush` is for accordions inside another bordered surface.

It removes the outer border and the corner radius so the item rows sit flat
against their container. Inside a `Card` that is what you want; on the bare page
it leaves the accordion with no edge to hold it.

</div>

<div className="sb-ds-doc-section">

## Accessibility

- **Each header is a real button.** React Bootstrap renders the header as a
  `button` carrying `aria-expanded`, wrapped in an `h2`. Tab reaches every
  header; Enter and Space toggle; the heading list shows the whole index.
- **The body is not associated with its header.** There is no `aria-controls` on
  the button and no `role="region"` or `aria-labelledby` on the body — it is a
  plain `div` that follows. In practice a screen reader lands on the body
  immediately after the header it just expanded, so the order carries the
  meaning. It does not survive being jumped to out of order.
- **Write `header` as a heading.** It becomes the text of an `h2`, so it is
  read both in place and in the heading list. A short noun phrase, not a
  sentence.
- **`disabled` stops the keyboard too.** The prop reaches the header button as
  a real `disabled` attribute (#331), so the section is out of the tab order and
  announced as unavailable rather than merely greyed. It still is not a way to
  withhold content: the body stays in the DOM, so leave the item out if what is
  inside it is not for this reader.
- **Focus stays on the header after a toggle.** Opening an item does not move
  focus into the body and does not scroll it into view. That is the right
  behaviour — the user keeps their place — but it means a tall body opened low
  in the viewport is off-screen until they scroll.

</div>

<div className="sb-ds-doc-section">

## Related

- For one section that opens and closes, use `Collapse` — an accordion of one
  item is furniture around a toggle.
- For switching between alternative views of the same content, use `NavTabs`.
  Tabs say "one of these"; an accordion says "these, some of which are open".
- For a list of items that navigate rather than expand, use `ListGroup`.
- When every section has to be readable at once, use none of them and lay the
  sections out. Comparing two collapsed items means opening, reading, closing,
  opening.

</div>

</div>

</DocsTab>

</DocsTabs>
