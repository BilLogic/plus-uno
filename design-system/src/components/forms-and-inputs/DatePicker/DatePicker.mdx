import { DocsTabs, DocsTab } from '@/storybook-docs/docs-tabs.jsx';
import { Meta, Title, Description, Canvas } from '@storybook/addon-docs/blocks';

import * as DatePickerStories from './DatePicker.stories.jsx';
import { DocsCanvasShell, DocsInteractivePlayground, ResourcesBlock, DsCanvasQuiet } from '@/storybook-docs/ds-docs-layout.jsx';

export const figmaMeta = {
  "fileKey": "zAecJNRdvJzAUOcjV32tRX",
  "sets": [
    {
      "id": "date-time-picker",
      "name": "Date & Time Picker",
      "componentSetNodeId": "13549:6703",
      "url": "https://www.figma.com/design/zAecJNRdvJzAUOcjV32tRX/Design-System---BS4-Foundation--Component-LIbrary-?node-id=13549-6703",
      "source": "manual",
      "status": "verified"
    },
    {
      "id": "month-date-picker",
      "name": "Month (Date Picker)",
      "componentSetNodeId": "13574:1150",
      "url": "https://www.figma.com/design/zAecJNRdvJzAUOcjV32tRX/Design-System---BS4-Foundation--Component-LIbrary-?node-id=13574-1150",
      "source": "manual",
      "status": "verified"
    },
    {
      "id": "date-picker-single-date",
      "name": "Date Picker (Single Date)",
      "componentSetNodeId": "9413:1764",
      "url": "https://www.figma.com/design/zAecJNRdvJzAUOcjV32tRX/Design-System---BS4-Foundation--Component-LIbrary-?node-id=9413-1764",
      "source": "node-chart",
      "status": "verified"
    },
    {
      "id": "date-picker-range",
      "name": "Date Picker (Range)",
      "componentSetNodeId": "9538:17087",
      "url": "https://www.figma.com/design/zAecJNRdvJzAUOcjV32tRX/Design-System---BS4-Foundation--Component-LIbrary-?node-id=9538-17087",
      "source": "node-chart",
      "status": "verified"
    }
  ]
};

<Meta of={DatePickerStories} />

<Title />
Use a date picker when a user needs to select a date or a date range.
Closed trigger is a **form field** with `MM/DD/YYYY` + **calendar-day** icon (not a Select caret).

<DocsTabs of={DatePickerStories}>

<DocsTab tab="examples">

<ResourcesBlock 
    figmaLink="https://www.figma.com/design/zAecJNRdvJzAUOcjV32tRX/Design-System---BS4-Foundation--Component-LIbrary-?node-id=13549-6703"
    githubLink="https://github.com/BilLogic/plus-uno/tree/main/design-system/src/components/forms-and-inputs/DatePicker"
    sets={figmaMeta.sets}
    figmaFileKey={figmaMeta.fileKey}
/>

<div className="sb-ds-component-docs sb-ds-component-docs--page not-prose">

<div className="sb-ds-doc-section">

## Overview

<DocsCanvasShell attachSourceBelow description={<>Medium trigger with placeholder — opens calendar on focus.</>}>
    <Canvas of={DatePickerStories.Overview} story={{ inline: true }} layout="padded" sourceState="hidden" />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Content

<DocsCanvasShell description={<>Single date selection with selected value summary.</>}>
    <DsCanvasQuiet of={DatePickerStories.PickDate} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Date ranges

<DocsCanvasShell description={<>Paired pickers for a start and end date range.</>}>
    <DsCanvasQuiet of={DatePickerStories.PickRange} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Sizes

<DocsCanvasShell description={<>Small, medium, and large.</>}>
    <DsCanvasQuiet of={DatePickerStories.Sizes} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## States

<DocsCanvasShell description={<>Default, filled, disabled, and read-only.</>}>
    <DsCanvasQuiet of={DatePickerStories.InteractionStates} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Interactive playground

<DocsInteractivePlayground
    of={DatePickerStories.Interactive}
    description={
        <>
            Use the table below to change props. The preview is rendered{' '}
            <strong className="text-foreground">inline</strong> while you scroll the docs page.
        </>
    }
/>

</div>

</div>

</DocsTab>

<DocsTab tab="usage">

<div className="sb-ds-component-docs sb-ds-component-docs--page not-prose">

<div className="sb-ds-doc-section">

## When to use

Use `DatePicker` for a single calendar date the user picks rather than types — a
session date, a deadline, a birthday within a bounded range. The trigger is a
read-only field; the calendar opens beneath it.

`value` and `onChange` speak ISO `YYYY-MM-DD`. The closed field displays
`MM/DD/YYYY`, and the two formats are not the same string — do not read what is
on screen as the value. `minDate` and `maxDate` accept either an ISO string or a
`Date`, and disable everything outside them.

</div>

<div className="sb-ds-doc-section">

## Usage notes

### The value is ISO. Anything else parses to empty.

```jsx
// Incorrect — parsing splits on "-" and requires three parts, so a slashed
// date returns null and the field silently shows the placeholder.
<DatePicker value="08/29/2026" onChange={setDate} />

// Correct.
<DatePicker value="2026-08-29" onChange={setDate} />
```

### It gives you a date, not a time.

`onChange` hands back the ISO day and nothing else. For a moment in time, use
`DateAndTimePicker`, which pairs the two fields and names them as one group.

### Label it the ordinary way.

```jsx
// The id lands on the input, so a label pointing at it works — unlike the other
// menu-backed fields in this group.
<label htmlFor="starts">Session date</label>
<DatePicker id="starts" value={date} onChange={setDate} />
```

### Bounds disable days; they do not clamp a value.

A `value` outside `minDate`/`maxDate` is still displayed and still returned. The
bounds only stop the user from choosing another one, so validate the incoming
value yourself.

</div>

<div className="sb-ds-doc-section">

## Accessibility

- **The trigger is wired correctly.** `role="combobox"` with
  `aria-haspopup="dialog"`, `aria-expanded`, and `aria-controls` pointing at the
  calendar. Enter, Space and Down all open it from the keyboard.
- **The days are real buttons.** Each is a `button` with a genuine `disabled`
  attribute outside the bounds, so they are reachable by Tab and announced as
  unavailable rather than doing nothing.
- **A day announces its whole date.** Each button is named with the weekday,
  day, month and year in the reader's locale, carries `aria-pressed` when it is
  the chosen day, and `aria-current="date"` when it is today (#331). The header's
  month and year are no longer the only context.
- **Adjacent-month days are dimmed, not unreadable.** They measure 7.66:1
  against the calendar surface — still lighter than the current month's 13.98:1,
  and above the 4.5:1 that 14px text needs.
- **There is no arrow-key movement inside the calendar.** Tab steps through the
  days one at a time; up to 31 tab stops to reach the end of a month.
- **`readOnly` and `disabled` both stop the calendar opening.** `disabled` takes
  the input out of the tab order; `readOnly` leaves it focusable and inert,
  which is the right difference between "not for you" and "not now".

</div>

<div className="sb-ds-doc-section">

## Related

- For a date and a time together, use `DateAndTimePicker`.
- For a date typed rather than picked, use `Input` with your own validation —
  a typed date is faster for anyone who knows it.
- For a range, use two pickers and bound the second with the first's value
  through `minDate`.

</div>

</div>

</DocsTab>

</DocsTabs>
