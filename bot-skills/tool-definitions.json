[
  {
    "name": "implement",
    "description": "Trigger the figma-implement.yml GitHub Action to open a draft pull request that implements a Figma component change. Use ONLY when the designer has clearly asked for a specific named component to be implemented (e.g., 'implement Badge', 'go ahead with the Badge change'). A component implement REQUIRES a Notion PRD — the polling bot creates one and posts it in #figma-sync. Before invoking: if the thread already has that PRD notification, proceed (the Worker reads the PRD from the thread). If there is NO PRD in the thread, do NOT invoke — ask the designer for the PRD link first, then pass it as notion_prd_url. Never implement a component without a PRD, and never invent the component name or PRD URL.",
    "input_schema": {
      "type": "object",
      "additionalProperties": false,
      "required": ["component"],
      "properties": {
        "component": {
          "type": "string",
          "description": "The component name as it appears in the Figma library, using the exact casing (e.g., 'Badge', 'Button', 'CardSurface')."
        },
        "notion_prd_url": {
          "type": "string",
          "description": "The Notion PRD link for this component change. Usually the Worker reads it automatically from the polling-bot notification in the thread — only set this when the designer pastes a PRD link themselves. Required overall: if no PRD is in the thread and the designer hasn't given one, ask before invoking."
        },
        "notes": {
          "type": "string",
          "description": "Optional free-form implementation notes from the designer. Pass through verbatim if present in the conversation; omit otherwise."
        }
      }
    }
  },
  {
    "name": "implement_design",
    "description": "Trigger the figma-implement-design.yml GitHub Action to scaffold a NEW playground prototype from a Figma design (page/screen/flow). Use when the designer pastes a Figma URL and asks for a prototype to be built (e.g., 'implement this design <figma.com/...>', 'scaffold a playground for this Figma frame'). A Notion PRD link is OPTIONAL but preferred: first ask the designer if they have a PRD link to include for better context — if they don't, offer to skip it and build straight from the Figma frame, and on their OK invoke this tool with notion_prd_url omitted. Never invent a PRD URL. Do NOT use this tool for DS library component work — that is `implement`, which is for named components downstream of the polling bot's PRD notification.",
    "input_schema": {
      "type": "object",
      "additionalProperties": false,
      "required": ["figma_url"],
      "properties": {
        "figma_url": {
          "type": "string",
          "description": "Full Figma URL the designer pasted, e.g. 'https://www.figma.com/design/NUcRDZUzUiuVmx1ydNXpl3/APPLICATION-PROTOTYPES?node-id=158-21725'. Must contain a node-id query param."
        },
        "notion_prd_url": {
          "type": "string",
          "description": "Optional. Full notion.so URL of a PRD for this prototype, if the designer has one — gives the build better context. Never invent it. Ask first; if they don't have one and choose to skip, omit this field."
        },
        "slug": {
          "type": "string",
          "description": "Optional kebab-case folder name for playground/{slug}/. Must match ^[a-z0-9][a-z0-9-]{1,40}$. If omitted, the script derives one from the Figma node name."
        },
        "notes": {
          "type": "string",
          "description": "Optional free-form implementation notes from the designer. Pass through verbatim."
        }
      }
    }
  },
  {
    "name": "create_prd",
    "description": "Create a design PRD as a new card on the Design HQ → Product board in Notion (the 'Roadmap' database), placed in the 'Need PRD / Under Playground' column and tagged for the Design team. Use when a designer asks to draft/write a PRD from a description, meeting notes, or a pasted discussion. WORKFLOW: first draft the PRD conversationally (as a text reply) and let the designer refine it — do NOT call this tool yet. Only invoke create_prd once the designer approves creating it (e.g. 'looks good, create it', 'make the PRD'). Pass the structured PRD content you drafted. Keep it grounded in the provided notes — put genuine unknowns under an 'Open Questions' section rather than inventing requirements.",
    "input_schema": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "summary"],
      "properties": {
        "title": {
          "type": "string",
          "description": "Short feature/PRD title — becomes the Roadmap card name."
        },
        "summary": {
          "type": "string",
          "description": "1-3 sentence overview of what this is and why."
        },
        "sections": {
          "type": "array",
          "description": "PRD body sections in order, e.g. Problem / Context, Goals & Non-goals, Users & Scenarios, Requirements & Scope, Open Questions. Each body may contain multiple paragraphs (separate with blank lines).",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["heading", "body"],
            "properties": {
              "heading": { "type": "string" },
              "body": { "type": "string" }
            }
          }
        },
        "acceptance_criteria": {
          "type": "array",
          "description": "Checklist of acceptance criteria — rendered as Notion to-do checkboxes.",
          "items": { "type": "string" }
        },
        "product_pillar": {
          "type": "string",
          "description": "Optional. One Roadmap Product Pillar if clearly implied: Universal, Admin, Toolkit, Training, Help Center, Tutoring, Goal-Setting, LID, Social Media, Website, Intercom, Others. Omit if unsure."
        },
        "source_url": {
          "type": "string",
          "description": "Optional. Link back to the source — a Slack permalink to the discussion, or a Figma link if one was provided."
        }
      }
    }
  },
  {
    "name": "delete_prd",
    "description": "Archive (delete) a PRD card on the Design HQ → Product board in Notion — use to undo/remove a PRD the bot just created when the designer asks ('delete that PRD', 'remove that card', 'undo that'). Pass the Notion URL of the card (the bot posted it when it created the PRD; or the designer pastes it). Archiving moves the card to Notion's trash (recoverable). Only works on cards in that board's database.",
    "input_schema": {
      "type": "object",
      "additionalProperties": false,
      "required": ["notion_url"],
      "properties": {
        "notion_url": {
          "type": "string",
          "description": "The Notion URL (or page id) of the Roadmap card to archive — typically the link the bot posted when it created the PRD."
        }
      }
    }
  },
  {
    "name": "marketplace_search",
    "description": "Search the Prototype Market catalog (prototypes-data.js). Use when the designer wants to find prototypes, list entries by some criterion, or look up a specific entry. Examples: 'show me toolkit prototypes', 'list high-fi prototypes', 'who built 1008?', \"find Bill's prototypes\". Read-only — safe to call freely, including before marketplace_edit to verify an entry exists.",
    "input_schema": {
      "type": "object",
      "additionalProperties": false,
      "required": ["query"],
      "properties": {
        "query": {
          "type": "string",
          "description": "Free-form query. Matched case-insensitively against title, description, productPillar, stage, creators, contributors, and id."
        }
      }
    }
  },
  {
    "name": "find_experts",
    "description": "READ-ONLY. Returns the team roster (name, role, bio, LinkedIn) from the Notion Team Member Database so you can suggest collaborators / subject-matter experts for a research topic. Use when a designer asks 'who should I talk to about X', 'find me SMEs/collaborators for X', or is sourcing context from people. After it returns, match the topic against each person's role + bio and present the best 2-4 fits with their role + a one-line reason + LinkedIn, then suggest reaching out. The DB has NO Slack handles/emails, so you cannot @-mention or DM anyone — name who to contact and share their LinkedIn; never tag. Safe to call freely (no side effect, no confirmation gate).",
    "input_schema": {
      "type": "object",
      "additionalProperties": false,
      "required": ["topic"],
      "properties": {
        "topic": {
          "type": "string",
          "description": "The research topic / area to find people for, e.g. 'AI student insights', 'onboarding for no-toolkit institutions'."
        }
      }
    }
  },
  {
    "name": "marketplace_add",
    "description": "Open a draft pull request that appends a new entry to the Prototype Market catalog. Use when the designer wants to register a new prototype. All required metadata fields MUST be present in the conversation before invoking — if any is missing, reply conversationally asking for it instead of calling this tool. Do not invent values. The Worker auto-assigns the 4-digit id, localPath, and lastUpdated; do not include those.",
    "input_schema": {
      "type": "object",
      "additionalProperties": false,
      "required": ["metadata"],
      "properties": {
        "metadata": {
          "type": "object",
          "additionalProperties": false,
          "required": ["title", "description", "stage", "productPillar", "creators", "repoPath"],
          "properties": {
            "title": { "type": "string" },
            "description": { "type": "string" },
            "stage": { "type": "string", "enum": ["low", "mid", "high"] },
            "productPillar": {
              "type": "string",
              "enum": ["admin", "home", "login", "profile", "toolkit", "training", "universal"]
            },
            "creators": {
              "type": "array",
              "items": { "type": "string" },
              "minItems": 1,
              "description": "Slack display names of the prototype's creators."
            },
            "contributors": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Optional. Slack display names of contributors beyond the creators."
            },
            "repoPath": {
              "type": "string",
              "description": "Path to the prototype directory in the plus-uno repo, e.g. 'playground/quick-add-modal/'."
            },
            "deploymentUrl": { "type": ["string", "null"], "description": "Optional. Deployed URL, or null." },
            "notionCardUrl":  { "type": ["string", "null"] },
            "notionCardId":   { "type": ["string", "null"] },
            "loomVideoUrl":   { "type": ["string", "null"] }
          }
        }
      }
    }
  },
  {
    "name": "marketplace_edit",
    "description": "Open a draft pull request that updates an existing Prototype Market entry. Use when the designer wants to change one or more fields of an existing entry. The id MUST reference an existing prototype — call marketplace_search first if you are uncertain. The fields object is a partial update; include only what should change.",
    "input_schema": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "fields"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Existing 4-digit prototype id, e.g. '1008'."
        },
        "fields": {
          "type": "object",
          "additionalProperties": false,
          "minProperties": 1,
          "properties": {
            "title": { "type": "string" },
            "description": { "type": "string" },
            "stage": { "type": "string", "enum": ["low", "mid", "high"] },
            "productPillar": {
              "type": "string",
              "enum": ["admin", "home", "login", "profile", "toolkit", "training", "universal"]
            },
            "creators": { "type": "array", "items": { "type": "string" }, "minItems": 1 },
            "contributors": { "type": "array", "items": { "type": "string" } },
            "repoPath": { "type": "string" },
            "deploymentUrl": { "type": ["string", "null"] },
            "notionCardUrl":  { "type": ["string", "null"] },
            "notionCardId":   { "type": ["string", "null"] },
            "loomVideoUrl":   { "type": ["string", "null"] }
          }
        }
      }
    }
  },
  {
    "name": "resolve_pending_proposal",
    "description": "Resolve a side-effect proposal that is awaiting user confirmation. Use ONLY when the system prompt indicates a pending proposal AND the user's current message clearly expresses a decision about it. Examples that should trigger this: 'go ahead', 'yes do it', 'looks good, ship it', 'no, cancel', 'actually nevermind'. If the user asks an unrelated question or seems to be discussing without deciding, do NOT invoke this tool — reply conversationally and the proposal will sit until ✅, ❌, or 15-minute expiry. If the system prompt indicates the current message sender is NOT the original requester, do NOT invoke this tool — reply explaining that only the requester can confirm.",
    "input_schema": {
      "type": "object",
      "additionalProperties": false,
      "required": ["decision"],
      "properties": {
        "decision": {
          "type": "string",
          "enum": ["confirm", "cancel"],
          "description": "'confirm' if the user wants to proceed with the proposed action as-is. 'cancel' if the user wants to stop, change direction, or modify parameters (if they want to change parameters, follow up with a fresh proposal in the same turn or wait for the user to clarify)."
        },
        "message_to_user": {
          "type": "string",
          "description": "Short Slack-formatted (mrkdwn) message to post alongside the resolution. Examples: 'Got it — firing the implement workflow for Badge now.' or 'Sure, cancelled. Let me know what you'd like to do instead.' Keep to one or two sentences."
        }
      }
    }
  }
]
