import { Meta, Title, Canvas } from '@storybook/addon-docs/blocks';

import * as SessionAdminStories from './SessionAdmin.stories.jsx';
import {
    DocsCanvasShell,
    DocsInteractivePlayground,
    ResourcesBlock,
} from '@/storybook-docs/ds-docs-layout.jsx';

import * as SessionAdminPageStories from '../Session Admin/Pages/SessionAdminPage/SessionAdminPage.stories.jsx';
import * as SessionOverviewSectionStories from '../Session Admin/Sections/SessionOverviewSection/SessionOverviewSection.stories.jsx';
import * as SessionsTableStories from '../Session Admin/Tables/SessionsTable/SessionsTable.stories.jsx';
import * as SessionBreakdownTableStories from '../Session Admin/Tables/SessionBreakdownTable/SessionBreakdownTable.stories.jsx';

<Meta of={SessionAdminStories} />

<Title />

The Session Admin spec is the unified surface for monitoring tutoring sessions — schedules, attendance, breakdowns by school/tutor/cohort, and per-session detail. Pages, sections, tables, and modals are bundled here.

<ResourcesBlock
    figmaLink="https://www.figma.com/design/W0qzhXWxFsMwSJzkdV2yal/Design-System---Web-App-Specs?node-id=300-113001"
    githubLink="https://github.com/BilLogic/plus-uno/tree/main/design-system/src/specs/Admin/Session%20Admin"
/>

<div className="sb-ds-component-docs sb-ds-component-docs--page not-prose">

<div className="sb-ds-doc-section">

### Overview

<DocsCanvasShell
    attachSourceBelow
    description={
        <>
            Session Admin uses the same shell as the other Admin specs — TopBar, sidebar, action buttons,
            and the session/breakdown workspace below.
        </>
    }
>
    <Canvas
        of={SessionAdminPageStories.Overview}
        story={{ inline: true }}
        layout="padded"
        sourceState="hidden"
    />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

### Pages

#### Session Admin page

<DocsInteractivePlayground
    of={SessionAdminPageStories.Interactive}
    description={<>Full session admin page — overview, sessions table, and the session modal layer.</>}
/>

</div>

<div className="sb-ds-doc-section">

### Sections

#### Session Overview section

<DocsInteractivePlayground
    of={SessionOverviewSectionStories.Interactive}
    description={<>KPI cards summarizing the active session cohort.</>}
/>

</div>

<div className="sb-ds-doc-section">

### Tables

#### Sessions table

<DocsInteractivePlayground
    of={SessionsTableStories.Interactive}
    description={<>Per-session log with date, tutor, school, attendance, and status columns.</>}
/>

#### Session Breakdown table

<DocsInteractivePlayground
    of={SessionBreakdownTableStories.Interactive}
    description={<>Aggregated breakdown by school / tutor / cohort.</>}
/>

</div>

<div className="sb-ds-doc-section">

### Modals

Modal stories render in an overlay layer, so they're listed here but linked out to their dedicated sidebar entries instead of embedded inline.

- **Session modal** — detail view for a single session with tabs for info, attendance, and notes. → `Specs/Admin/Session Admin/Modals/SessionModal`.

</div>

</div>
