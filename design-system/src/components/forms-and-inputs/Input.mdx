import { DocsTabs, DocsTab } from '@/storybook-docs/docs-tabs.jsx';
import { Meta, Title, Description, Canvas } from '@storybook/addon-docs/blocks';

import * as InputStories from './Input.stories.jsx';
import { DocsCanvasShell, DocsInteractivePlayground, ResourcesBlock, DsCanvasQuiet } from '@/storybook-docs/ds-docs-layout.jsx';

export const figmaMeta = {
  "fileKey": "zAecJNRdvJzAUOcjV32tRX",
  "sets": [
    {
      "id": "input",
      "name": "Input",
      "componentSetNodeId": "39:4802",
      "url": "https://www.figma.com/design/zAecJNRdvJzAUOcjV32tRX/Design-System---BS4-Foundation--Component-LIbrary-?node-id=39-4802",
      "source": "node-chart",
      "status": "verified"
    }
  ]
};

<Meta of={InputStories} />

<Title />
The labeled text input box allows users to enter textual inputs and receive validation feedback with messages.

<DocsTabs of={InputStories}>

<DocsTab tab="examples">

<ResourcesBlock 
    figmaLink="https://www.figma.com/design/zAecJNRdvJzAUOcjV32tRX/Design-System---BS4-Foundation--Component-LIbrary-?node-id=13611-14133"
    githubLink="https://github.com/BilLogic/plus-vibe-coding-starting-kit/tree/main/design-system/src/components/forms-and-inputs"
    sets={figmaMeta.sets}
    figmaFileKey={figmaMeta.fileKey}
/>

<div className="sb-ds-component-docs sb-ds-component-docs--page not-prose">

<div className="sb-ds-doc-section">

## Overview

<DocsCanvasShell attachSourceBelow description={<>Default labeled field with placeholder and trailing visual.</>}>
    <Canvas of={InputStories.Overview} story={{ inline: true }} layout="padded" sourceState="hidden" />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Content

<DocsCanvasShell description={<>Placeholder vs value, label visibility, required marker, and leading/trailing visuals.</>}>
    <DsCanvasQuiet of={InputStories.Content} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Validation & feedback

<DocsCanvasShell description={<>Validation treatments: none, invalid, and success (with optional message).</>}>
    <DsCanvasQuiet of={InputStories.Styles} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Sizes

<DocsCanvasShell description={<>Available in medium (default) size.</>}>
    <DsCanvasQuiet of={InputStories.Sizes} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## States

<DocsCanvasShell description={<>Default, hover, focus, disabled, and read-only.</>}>
    <DsCanvasQuiet of={InputStories.InteractionStates} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Interactive playground

<DocsInteractivePlayground
    of={InputStories.Interactive}
    description={
        <>
            Use the table below to change props. The preview is rendered{' '}
            <strong className="text-foreground">inline</strong> while you scroll the docs page.
        </>
    }
/>

</div>

</div>

</DocsTab>

<DocsTab tab="usage">

<div className="sb-ds-component-docs sb-ds-component-docs--page not-prose">

<div className="sb-ds-doc-section">

## When to use

Use `Input` for single-line text entry — names, emails, search boxes, anything
where the answer is one short string. It is the field that owns its own label,
its own validation state and its own message, which makes it the right home for
an error rather than an `Alert` at the top of the form.

`validation` takes `none`, `invalid` or `success`; pair it with
`validationMessage` and the component renders the message beneath the field with
a matching icon.

</div>

<div className="sb-ds-doc-section">

## Accessibility

- **The label is associated whether you pass `id` or not.** One id is derived
  once and used for both the label's `htmlFor` and the input; when no `id` is
  given, React's `useId` supplies one. Pass `id` when something outside the
  component needs to point at the input — an `aria-describedby`, a `<label>` you
  render yourself, a test selector — and you will get exactly the id you passed.
  `name` is a form key, not an id, and no longer stands in for one (#206).
- **`showLabel={false}` hides the label, it does not remove it.** The label is
  rendered either way and always carries `htmlFor`; `showLabel={false}` only
  clips it out of sight with `.plus-input-label-hidden`. So a field that has a
  `label` has an accessible name whether or not you show it, and turning the
  label off costs nothing but the pixels. This was the other way round until
  #213 — see *Why `showLabel` cannot take the name away* below.
- **`required` is visual only.** It renders a `*` span marked
  `aria-label="required"` and is not forwarded to the input.
- **`validation` is announced, not only drawn.** The message gets an id, the
  input points at it with `aria-describedby`, and `validation="invalid"` sets
  `aria-invalid` (#327). A `success` message describes without claiming an
  error. Your own `aria-describedby` is kept and the message id is appended, so
  standing help and a new error are both read, in that order.
- **A field with no `label` still has no name.** `showLabel` decides whether the
  label is *seen*, not whether there is one. If the visible label is rendered by
  the caller instead — a `<Label>` above the field, a table column header — point
  it at the input with `htmlFor`; if there is no text anywhere, pass `aria-label`.
- **Everything else passes through.** `type`, `autoComplete`, `maxLength`,
  `pattern`, `inputMode` and any `aria-*` attribute all reach the input element,
  so the native affordances are available even though the wrapper does not use
  them.

### Always pass `id`, or the label points at nothing.

```jsx
// Incorrect — the label's htmlFor falls back to `name`, but `name` is not used
// as the input's id. The label ends up pointing at an element that does not
// exist, and clicking it does nothing.
<Input name="email" label="Email address" value={email} onChange={onEmail} />

// Correct — id is what the input actually receives.
<Input id="email" name="email" label="Email address" value={email} onChange={onEmail} />
```

### `required` draws an asterisk; it does not mark the field required.

```jsx
// Incorrect — the star appears, but the input carries neither the `required`
// attribute nor `aria-required`, so nothing outside the visual layer knows.
<Input id="school" label="School" required value={school} onChange={onSchool} />

// Correct — unknown props are spread onto the input, so declare it as well.
<Input id="school" label="School" required aria-required="true"
       value={school} onChange={onSchool} />
```

### `validation` styles the field; it does not announce the error.

```jsx
// Incorrect — red border, red message, and a screen reader that hears none
// of it. `validation` only adds classes.
<Input id="email" label="Email" validation="invalid"
       validationMessage="Enter a valid email address." />

// Correct — say it in ARIA too, and give the message an id to point at.
<Input id="email" label="Email" validation="invalid"
       validationMessage="Enter a valid email address."
       aria-invalid={true} aria-describedby="email-error" />
```

</div>

<div className="sb-ds-doc-section">

## Why `showLabel` cannot take the name away

`showLabel={false}` used to skip rendering the label entirely. #213 asked
whether that mode should exist at all, since a component that makes "no
accessible name" easy keeps producing the defect. The 37 call sites answered it,
and they were not one population:

- **17 pass a real `label` string and then switch it off** — the whole Login
  family (`LoginPortal`, `InstitutionSelection`, `InstitutionForm`,
  `AccessCodeForm`) and the prototype-market search field. The name was already
  written; the design just has no room to show it. These never wanted "no
  label", only "no visible label".
- **19 pass no `label` at all**, because the caller renders its own visible
  label above the field — the Profile sections do this throughout, with the
  `Label` element from Label and caption. `showLabel={false}` there prevents a
  *second* label, which is a real and correct use. Removing the prop would put a
  duplicate label back on every one of those fields.
- **1 is a search box inside `Select`'s dropdown**, named by the listbox it
  filters.

So neither answer in the ticket was right on its own. Forbidding the mode breaks
the 19; permitting it as-is leaves the 17 one careless call site away from an
unnamed field forever. The resolution is to make the mode mean something it can
be trusted with: **`showLabel` is a visual switch, and the accessibility tree
does not read it.** The 17 gained a name with no visual change and no call-site
edit; the 19 are untouched, because a `label` you never passed is still not
rendered. What is left is the one honest way to have no name — pass no text at
all — which is true of every component in the system and is not something
`Input` makes especially easy.

The half that `Input` cannot fix is the 19: a label rendered outside the
component has to be pointed at the input by the caller. Seventeen of them pass
`htmlFor` — the Profile sections and the tutor profile page — because each names
one control.

The last two were `DateAndTimePicker`s under a single shared caption, where
`htmlFor` was never available: one `<label for>` names one control, and that
field renders two. #225 settled them from the other end. The visual switch was
ported — `DateAndTimePicker`'s `showLabel` now clips its label instead of
deleting it, same clipping, same reasoning — and each picker was given its own
`label` to clip, so the two controls are named "Start time" and "End time"
rather than both being called "Time". The visible caption stopped competing for
that job and names the pair as a `group`. In both components the name now
belongs to the component; text the caller renders above the field is a caption,
not a second claim on the name.

</div>

<div className="sb-ds-doc-section">

## Related

- For multi-line text, use `Textarea`.
- For numbers with steppers, use `NumberInput`.
- For a value chosen from a list, use `Select`.
- For dates, use `DatePicker` or `DateAndTimePicker`.
- For an input with an attached button or addon, use `InputGroup`.

</div>

</div>

</DocsTab>

</DocsTabs>
