import React from 'react';
import StudentProgressPage from './StudentProgressPage';

/**
 * Entry component for the Student Progress prototype.
 * One screen — the prototype has no routing by design (see the manifest).
 */
const App = () => <StudentProgressPage />;

export default App;
