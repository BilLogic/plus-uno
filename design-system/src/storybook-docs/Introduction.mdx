import { Meta } from '@storybook/addon-docs/blocks';
import LinkTo from '@storybook/addon-links/react';
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from '@/storybook-docs/ui/card';
import { DocHero, DocPage, DocSection } from '@/storybook-docs/doc-layout';

<Meta
  title="PLUS Docs/Introduction"
  parameters={{
    docs: {
      description: {
        story: 'Start here for how this Storybook is organized and how to review components.',
      },
    },
  }}
/>

<DocPage width="narrow">
  <DocHero
    title="Introduction"
    lead="This Storybook documents the PLUS design system: tokens, patterns, shared components, and
      product specs. Documentation pages use a neutral layout for reading; the canvas still renders
      real components with Bootstrap and design-system styles."
  />

  <div className="grid gap-4 md:grid-cols-2">
    <Card>
      <CardHeader>
        <CardTitle>Open the canvas</CardTitle>
        <CardDescription>Inspect states, controls, and viewports.</CardDescription>
      </CardHeader>
      <CardContent className="text-sm text-muted-foreground">
        Use the toolbar to change background, viewport, and globals. Controls map to component props
        where stories define <code className="rounded bg-muted px-1 py-0.5 text-xs">argTypes</code>
        .
      </CardContent>
    </Card>
    <Card>
      <CardHeader>
        <CardTitle>Read the docs tab</CardTitle>
        <CardDescription>Usage notes and autodocs where enabled.</CardDescription>
      </CardHeader>
      <CardContent className="text-sm text-muted-foreground">
        Each story file can ship markdown descriptions; many components use autodocs for props and
        snippets. Long pages show a table of contents on the right when headings are present.
      </CardContent>
    </Card>
  </div>

  <DocSection title="Who this is for">
    <ul className="ml-6 list-disc space-y-2 text-sm text-muted-foreground">
      <li>Designers reviewing visual and interaction patterns</li>
      <li>Engineers implementing or extending shared UI</li>
      <li>PMs and partners orienting to what the system already provides</li>
    </ul>
  </DocSection>

  <DocSection title="Where to go next">
    <p className="text-sm leading-relaxed text-muted-foreground">
      Start with{' '}
      <LinkTo
        className="text-foreground underline hover:no-underline"
        title="PLUS Docs/How are Components Organized?"
        name="Docs"
      >
        How are Components Organized?
      </LinkTo>{' '}
      to learn the docs structure, then open{' '}
      <LinkTo className="text-foreground underline hover:no-underline" title="PLUS Docs/What are tokens?" name="Docs">
        What are tokens?
      </LinkTo>{' '}
      for foundations.
    </p>
    <p className="mt-3 text-sm leading-relaxed text-muted-foreground">
      Then browse the sidebar groups directly: <strong className="text-foreground">Styles</strong>,{' '}
      <strong className="text-foreground">Components</strong>,{' '}
      <strong className="text-foreground">Forms</strong>, and{' '}
      <strong className="text-foreground">Specs</strong>.
    </p>
  </DocSection>

  <DocSection title="Start here in 60 seconds">
    <ol className="ml-6 list-decimal space-y-2 text-sm text-muted-foreground">
      <li>Use Docs pages for scan-friendly reading; use Canvas when you need to test state and controls.</li>
      <li>
        For Components and Forms, expect the same section spine: <code className="rounded bg-muted px-1 py-0.5 text-xs">Content</code>{' '}
        - <code className="rounded bg-muted px-1 py-0.5 text-xs">Styles</code> -{' '}
        <code className="rounded bg-muted px-1 py-0.5 text-xs">Sizes</code> -{' '}
        <code className="rounded bg-muted px-1 py-0.5 text-xs">Interaction States</code> -{' '}
        <code className="rounded bg-muted px-1 py-0.5 text-xs">Layout</code> -{' '}
        <code className="rounded bg-muted px-1 py-0.5 text-xs">Interactive</code>.
      </li>
      <li>Use Controls in the Interactive section to validate prop behavior before implementation.</li>
    </ol>
  </DocSection>

  <DocSection title="Run Storybook locally">
    <ul className="ml-6 list-disc space-y-2 text-sm text-muted-foreground">
      <li>
        Install and start Storybook: <code className="rounded bg-muted px-1 py-0.5 text-xs">npm install</code>{' '}
        then <code className="rounded bg-muted px-1 py-0.5 text-xs">npm run storybook</code>
      </li>
      <li>
        Storybook runs on <code className="rounded bg-muted px-1 py-0.5 text-xs">http://localhost:4200</code>
      </li>
      <li>
        For app + Storybook together, run <code className="rounded bg-muted px-1 py-0.5 text-xs">npm run dev</code>{' '}
        (Vite on 4100, Storybook on 4200)
      </li>
    </ul>
  </DocSection>
</DocPage>
