import React from 'react';
import SessionsPage from '../playground/prototyping/sessions/SessionsPage';

// A simple App component to serve as the entry point
const App = () => {
    return <SessionsPage />;
};

export default App;
