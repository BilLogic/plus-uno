import { DocsTabs, DocsTab } from '@/storybook-docs/docs-tabs.jsx';
import { Meta, Title, Description, Canvas } from '@storybook/addon-docs/blocks';

import * as DropdownStories from './Dropdown.stories.jsx';
import { DocsCanvasShell, DocsInteractivePlayground, ResourcesBlock, DsCanvasQuiet } from '@/storybook-docs/ds-docs-layout.jsx';

export const figmaMeta = {
  "fileKey": "zAecJNRdvJzAUOcjV32tRX",
  "sets": [
    {
      "id": "dropdown-button",
      "name": "Dropdown button",
      "componentSetNodeId": "37:4513",
      "url": "https://www.figma.com/design/zAecJNRdvJzAUOcjV32tRX/Design-System---BS4-Foundation--Component-LIbrary-?node-id=37-4513",
      "source": "node-chart",
      "status": "verified"
    },
    {
      "id": "dropdown-list",
      "name": "Dropdown List",
      "componentSetNodeId": "9109:14766",
      "isComponentSet": false,
      "url": "https://www.figma.com/design/zAecJNRdvJzAUOcjV32tRX/Design-System---BS4-Foundation--Component-LIbrary-?node-id=9109-14766",
      "source": "node-chart",
      "status": "verified"
    },
    {
      "id": "dropdown",
      "name": "Dropdown",
      "componentSetNodeId": "4457:25458",
      "url": "https://www.figma.com/design/zAecJNRdvJzAUOcjV32tRX/Design-System---BS4-Foundation--Component-LIbrary-?node-id=4457-25458",
      "source": "node-chart",
      "status": "verified"
    }
  ]
};

<Meta of={DropdownStories} />

<Title />
Toggle contextual overlays for displaying lists of links and more with the Bootstrap dropdown plugin.

<DocsTabs of={DropdownStories}>

<DocsTab tab="examples">

<ResourcesBlock 
    figmaLink="https://www.figma.com/design/zAecJNRdvJzAUOcjV32tRX/Design-System---BS4-Foundation--Component-LIbrary-?node-id=37-4513"
    githubLink="https://github.com/BilLogic/plus-vibe-coding-starting-kit/tree/main/design-system/src/components/forms-and-inputs/Dropdown"
    sets={figmaMeta.sets}
    figmaFileKey={figmaMeta.fileKey}
/>

<div className="sb-ds-component-docs sb-ds-component-docs--page not-prose">

<div className="sb-ds-doc-section">

## Overview

<DocsCanvasShell attachSourceBelow description={<>Closed and open states for single-button and split-button dropdowns, aligned to the core Figma content examples.</>}>
    <Canvas of={DropdownStories.Overview} story={{ inline: true }} layout="padded" sourceState="hidden" />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Content

<DocsCanvasShell description={<>Single and split triggers in closed and open states.</>}>
    <DsCanvasQuiet of={DropdownStories.Content} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Appearance

<DocsCanvasShell description={<>Semantic roles for the trigger, matching the Dropdown variants frame in Figma (outline default; ghost shown as the alternate treatment).</>}>
    <DsCanvasQuiet of={DropdownStories.StyleVariants} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Sizes

<DocsCanvasShell description={<>Small, default, and large triggers.</>}>
    <DsCanvasQuiet of={DropdownStories.Sizes} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Layout & composition

<DocsCanvasShell description={<>Split button and menu opening direction.</>}>
    <DsCanvasQuiet of={DropdownStories.Layout} />
</DocsCanvasShell>

</div>

<div className="sb-ds-doc-section">

## Interactive playground

<DocsInteractivePlayground
    of={DropdownStories.Interactive}
    description={
        <>
            Use the table below to change props. The preview is rendered{' '}
            <strong className="text-foreground">inline</strong> while you scroll the docs page.
        </>
    }
/>

</div>

</div>

</DocsTab>

<DocsTab tab="usage">

<div className="sb-ds-component-docs sb-ds-component-docs--page not-prose">

<div className="sb-ds-doc-section">

## When to use

Use `Dropdown` for a menu of *actions* hanging off a control — a row's overflow
menu, a "more" button, a set of export formats. Items are described as data
(`items`), each with its own `onClick`, and the menu closes after a click unless
that item opts out.

It is the only component in `forms-and-inputs` that places itself: the menu
flips up when there is no room below and right-aligns when a left-aligned menu
would run off the edge, remeasuring on scroll and resize.

</div>

<div className="sb-ds-doc-section">

## Usage notes

### Controlled mode needs both halves.

```jsx
// Incorrect — `isOpen` without `onToggle` freezes the component. The user's
// clicks are reported to nobody, so nothing ever sets `open` back to false.
<Dropdown isOpen={open} buttonText="Actions" items={items} />

// Correct — take the state, and take the callback that moves it.
<Dropdown isOpen={open} onToggle={setOpen} buttonText="Actions" items={items} />

// Also correct — let the component own its own open state.
<Dropdown buttonText="Actions" items={items} />
```

### A menu of actions is not a picker.

```jsx
// Incorrect — the selection is not stored anywhere. `selected` is a per-item
// display flag the caller has to keep in sync by hand, and the trigger keeps
// showing "Sort by".
<Dropdown
  buttonText="Sort by"
  items={[{ text: 'Newest', onClick: () => setSort('newest') }]}
/>

// Correct — a value the user picks belongs in Select.
<Select
  id="sort"
  options={[{ value: 'newest', label: 'Newest' }]}
  value={sort}
  onChange={setSort}
/>
```

</div>

<div className="sb-ds-doc-section">

## Accessibility

- **The toggle is well formed.** It is a real `button` with `aria-haspopup` and
  a live `aria-expanded`. In `split` mode it also gets an `aria-label` built
  from `buttonText` plus the word "options", which is why the toggle half of a
  split button renders no visible text.
- **The menu is not an ARIA menu.** The container is a plain div and each item is
  a `button`; there is no `role="menu"` or `role="menuitem"`, no arrow-key
  navigation, and no Escape-to-close. Items are reached by tabbing, which works
  but does not match the menu pattern a screen-reader user expects.
- **Closing is mouse-driven.** The outside-click listener is bound to
  `mousedown` only. A keyboard user leaves the menu by tabbing past it, not by
  dismissing it.
- **A custom `toggle` loses keyboard access.** The node you pass is wrapped in a
  `div` with an `onClick` and no role, tabindex or key handler — so it is
  clickable and nothing else. Pass a `Button` as the trigger only if you are
  willing to lose that; otherwise use the built-in toggle.
- **Icons are decorative — mostly.** The selected check mark, the multi-select
  checkbox and `leadingIcon` are all `aria-hidden`. `trailingIcon` and the
  submenu caret are not, so an icon font glyph there can leak into the item's
  announced name. Keep the item's meaning in `text` either way.

### Give the split treatment a real button when the label must act.

```jsx
// Incorrect — clicking "Save" does nothing. The label half of a split button
// carries no handler and takes no prop for one.
<Dropdown split buttonText="Save" items={saveOptions} />

// Correct — a real Button beside a Dropdown for the extra options.
<Button text="Save" onClick={save} />
<Dropdown buttonText="Save options" items={saveOptions} />
```

</div>

<div className="sb-ds-doc-section">

## Related

- To hold a form value, use `Select` — a dropdown menu is a set of actions, not a field.
- For a long list the user needs to search, use `Select` with filtering.
- For rich content anchored to a trigger, use `Popover`.

</div>

</div>

</DocsTab>

</DocsTabs>
